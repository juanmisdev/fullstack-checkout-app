# Committed default: E2E runs against the deployed sandbox API so the real
# checkout flow works without a local backend. If you run the backend locally
# (localhost:3001), override this in .env.development.local (gitignored).
VITE_API_URL=https://dp2txvb8v8.execute-api.us-east-1.amazonaws.com/api/v1